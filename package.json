{
  "name": "Black-Music-Album-Review",
  "private": true,
  "version": "1.10.24",
  "license": "Apache 2.0",
  "scripts": {
    "develop": "gatsby develop -H 0.0.0.0",
    "develop:clean": "yarn clean && yarn develop",
    "build": "gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "build:analyze": "ANALYZE=true yarn build",
    "build:prefix": "gatsby build --prefix-paths",
    "serve:prefix": "gatsby serve --prefix-paths",
    "test:prefix": "yarn build:prefix && yarn serve:prefix",
    "cf login": "cf login -a https://api.us-south.cf.cloud.ibm.com --sso",
    "cf push": "cf push -f manifest.yml"
  },
  "dependencies": {
    "@carbon/pictograms-react": "^11.22.0",
    "carbon-components": "^10.53.0",
    "carbon-components-react": "^7.53.0",
    "carbon-icons": "^7.0.7",
    "gatsby": "4.7.2",
    "gatsby-plugin-google-analytics": "^4.7.0",
    "gatsby-theme-carbon": "2.3.0",
    "react": "17.0.2",
    "react-dom": "^17.0.2",
    "yarn": "^1.22.17"
  }
}
