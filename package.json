{
  "name": "Black-Music-Album-Review",
  "private": true,
  "version": "1.00.03",
  "license": "Apache 2.0",
  "scripts": {
    "develop": "gatsby develop -H 0.0.0.0",
    "develop:clean": "yarn clean && yarn develop",
    "build": "gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "build:analyze": "ANALYZE=true yarn build",
    "build:prefix": "gatsby build --prefix-paths",
    "serve:prefix": "gatsby serve --prefix-paths",
    "test:prefix": "yarn build:prefix && yarn serve:prefix"
  },
  "dependencies": {
    "@carbon/pictograms-react": "^11.1.0",
    "gatsby": "2.30.1",
    "gatsby-plugin-google-analytics": "^2.9.0",
    "gatsby-theme-carbon": "1.27.2",
    "react": "17.0.1",
    "react-dom": "^17.0.1"
  }
}